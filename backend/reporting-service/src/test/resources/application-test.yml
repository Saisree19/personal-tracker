spring:
  application:
    name: reporting-service
  r2dbc:
    url: r2dbc:h2:mem:///reportingdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: never
security:
  jwt:
    secret: test-secret-key-32-characters-12345
    ttl: PT1H
